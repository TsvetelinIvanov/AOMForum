@page
@model ShowRecoveryCodesModel
@{
    this.ViewData["Title"] = "Recovery codes";
    this.ViewData["ActivePage"] = "TwoFactorAuthentication";
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@this.ViewData["Title"]</h3>
<div class="alert alert-warning" role="alert">
    <p>
        <strong>Put these codes in a safe place.</strong>
    </p>
    <p>
        If you lose your device and don't have the recovery codes you will lose access to your account.
    </p>
</div>
<div class="row">
    <div class="col-md-12">
        @for (int row = 0; row < this.Model.RecoveryCodes.Length; row += 2)
        {
            <code class="recovery-code">@this.Model.RecoveryCodes[row]</code><text>&nbsp;</text><code class="recovery-code">@this.Model.RecoveryCodes[row + 1]</code><br />
        }
    </div>
</div>